syntax = "proto3";

package systems.ajax.a911.grpc.v1.hub.main.additional.data;

option java_package = "systems.ajax.a911.grpc.v1.hub.main.additional.data";
option java_multiple_files = true;

message MultipleGroups { // 1 type in the HtS. // added just in case for the future
  repeated CmsGroup cms_groups = 1;

  message CmsGroup {
    // upper case 1b HEX
    string group_number = 1;
    optional string group_name = 2;
  }
}
